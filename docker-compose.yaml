services:
  api:
    build:
      context: .
    image: meshcore-ninja-api:local
    ports:
      - "8080:8080"
    volumes:
      - ./config.toml:/app/config.toml:ro
      - ./data:/app/state
    restart: unless-stopped
